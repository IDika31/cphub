# ============================================================
# CPHub V4 — Environment Configuration
# ============================================================
# Copy file ini ke .env dan sesuaikan nilainya
#   cp .env.example .env
# ============================================================

# --- Database (native PostgreSQL) ---
DB_HOST=localhost
DB_PORT=5432
DB_USER=cphub
DB_PASSWORD=cphub
DB_NAME=cphub
DB_SSLMODE=disable
DB_TIMEZONE=Asia/Jakarta

# --- Redis (native) ---
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_PASSWORD=
REDIS_DB=0

# --- API Server ---
API_PORT=3001
API_HOST=0.0.0.0
API_BASE_URL=http://localhost:3001

# --- Frontend ---
WEB_PORT=3000
NEXT_PUBLIC_API_URL=http://localhost:3001

# --- JWT ---
JWT_SECRET=change-me-to-a-random-256-bit-secret
JWT_ACCESS_EXPIRY=15m
JWT_REFRESH_EXPIRY=7d

# --- Google OAuth ---
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
GOOGLE_REDIRECT_URL=http://localhost:3001/api/auth/google/callback

# --- Codeforces OIDC ---
CF_CLIENT_ID=
CF_CLIENT_SECRET=
CF_REDIRECT_URL=http://localhost:3001/api/auth/codeforces/callback

# --- Extension HMAC ---
EXTENSION_HMAC_SECRET=change-me-to-a-random-256-bit-secret

# --- Grader ---
GRADER_MAX_CONCURRENT=5
GRADER_TIMEOUT_SECONDS=5
GRADER_MEMORY_LIMIT_MB=512
GRADER_MAX_OUTPUT_KB=10
GRADER_MAX_CODE_SIZE_KB=256
GRADER_TEMP_DIR=/tmp/cphub-grader
GRADER_FIREJAIL_PROFILE=/etc/firejail/cphub.local

# --- Compiler Paths (Arch Linux CachyOS) ---
GCC_PATH=/usr/bin/g++
PYTHON3_PATH=/usr/bin/python3
NODE_PATH=/usr/bin/node
JAVAC_PATH=/usr/bin/javac
JAVA_PATH=/usr/bin/java
FIREJAIL_PATH=/usr/bin/firejail

# --- Logging ---
LOG_LEVEL=debug
LOG_FORMAT=text

# --- Environment ---
ENVIRONMENT=development
